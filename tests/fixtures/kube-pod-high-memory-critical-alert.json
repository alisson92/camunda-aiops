{
  "receiver": "camunda-aiops-webhook",
  "status": "firing",
  "alerts": [
    {
      "status": "firing",
      "labels": {
        "alertname": "KubePodHighMemoryCritical",
        "namespace": "camunda",
        "severity": "critical",
        "component": "pod",
        "team": "platform"
      },
      "annotations": {
        "summary": "Pod {{ $labels.namespace }}/{{ $labels.pod }} com memória > 90% do limit — risco de OOM",
        "description": "Container {{ $labels.container }} no pod {{ $labels.namespace }}/{{ $labels.pod }}\nestá usando {{ $value | humanizePercentage }} do memory limit.\nOOM kill iminente — ação imediata necessária.\n",
        "runbook_url": "http://172.18.0.1:5001/runbook/by-alert/KubePodHighMemoryCritical"
      },
      "startsAt": "2026-01-01T00:00:00Z",
      "endsAt": "0001-01-01T00:00:00Z",
      "generatorURL": "http://localhost:9090/graph?g0.expr=KubePodHighMemoryCritical"
    }
  ],
  "groupLabels": {
    "alertname": "KubePodHighMemoryCritical"
  },
  "commonLabels": {
    "alertname": "KubePodHighMemoryCritical",
    "severity": "critical"
  },
  "commonAnnotations": {
    "summary": "Pod {{ $labels.namespace }}/{{ $labels.pod }} com memória > 90% do limit — risco de OOM"
  },
  "externalURL": "http://localhost:9093",
  "version": "4",
  "groupKey": "{}/{alertname=~\".*\"}:{alertname=\"KubePodHighMemoryCritical\"}"
}
